# Development shell layer

The dev shell fragments add advanced Git workflows, language-tool initialization,
Direnv, Sley, and public agent wrappers. They rely on the loader and generic
tool-cache interface inherited from the base profile.
