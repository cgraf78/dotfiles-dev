# Development environment

Ordered files under `home/.config/shell/env.d/` provide development-only
environment variables. Credentials are read only from an existing user-owned
file; no credential or site-specific value is stored in this repository.
