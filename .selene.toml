# Repository-only lint context for the dev-owned Neovim modules. The deployed
# global fallback remains byte-identical to the frozen source policy.
std = "vim+lua51"

[rules]
unused_variable = "allow"
bad_string_escape = "allow"
mixed_table = "allow"
global_usage = "allow"
