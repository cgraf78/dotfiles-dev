# Development capability tests

The top-level executable suites in the parent directory exercise only behavior
owned by `dotfiles-dev`. Shared support lives in this directory because the
capability repository is tested without assembling the base or editor source
trees.
