#!/usr/bin/env bash
# pre-merge-commit — activate Sley's automatic-merge commit gate.

set -u

_hook_dir=$(cd -P -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd) || exit 0
exec "$_hook_dir/sley-provider-hook" pre-merge-commit
