#!/usr/bin/env bash
# pre-applypatch — activate Sley's `git am` commit gate.

set -u

_hook_dir=$(cd -P -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd) || exit 0
exec "$_hook_dir/sley-provider-hook" pre-applypatch
