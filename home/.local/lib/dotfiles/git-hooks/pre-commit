#!/usr/bin/env bash
# pre-commit — Git pre-commit hook.
# Activates Sley's repository-owned implementation.
#
# Last resort: git commit --no-verify

set -u

_hook_dir=$(cd -P -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd) || exit 0
exec "$_hook_dir/sley-provider-hook" pre-commit
