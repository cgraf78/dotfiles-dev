#!/usr/bin/env bash
# prepare-commit-msg — activate Sley's Git-sequencer commit gate.

set -u

_hook_dir=$(cd -P -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd) || exit 0
exec "$_hook_dir/sley-provider-hook" prepare-commit-msg "$@"
