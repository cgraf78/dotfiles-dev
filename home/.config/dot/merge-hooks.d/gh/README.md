# GitHub CLI Merge Hook

This directory declares the `gh` merge-hook instance. Its declarative source is
the ordered `config.d/` family in this directory.

The executable hook implementation lives at
`~/.local/lib/dotfiles/merge-hooks.d/gh.sh`.
